version: '3.8'

services:
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    depends_on:
#      - sqlserver
#    ports:
#      - "8000:8000"

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2017-latest
    environment:
      SA_PASSWORD: admin123!
      ACCEPT_EULA: Y
      MSSQL_PID: Express
    ports:
      - "1433:1433"